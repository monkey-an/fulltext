$("#downLoadPdf").on("click",function(){
    if(confirm("去充值")) {
        window.open($(this).attr("data"));
    }
});

$("#wholeDownLoad").on("click",function(){
    if(confirm("去充值")) {
        window.open($(this).attr("data"));
    }
});

$("#downLoadWord").on("click",function(){
    if(confirm("去充值")) {
        window.open($(this).attr("data"));
    }
});